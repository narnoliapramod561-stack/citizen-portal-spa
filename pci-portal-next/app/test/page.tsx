export default function TestPage() {
  return (
    <div className="p-20 text-white">
      <h1>Deployment Test - CSS/JS is working!</h1>
      <p>If you see this, the routing is working but the root page might have SSR issues.</p>
    </div>
  );
}
